
# Logs
*.log
npm-debug.*

# Tests coverage (jest --coverage)
coverage/

# Uploads legacy (disk storage). Las imágenes ya no se guardan acá:
# ahora viven en Supabase Storage (bucket SUPABASE_BUCKET_MEDIA).
# Esta carpeta queda ignorada para que archivos residuales no se
# versionen. Se puede hacer `git rm -r --cached backend-subasta/uploads`
# para limpiar el tracking histórico.
uploads/
